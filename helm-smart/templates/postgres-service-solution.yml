apiVersion: v1
kind: Service
metadata:
  name: mypostgres-service
spec:
  selector:
    app: mypostgres
  ports:
    - protocol: TCP
      port: {{ .Values.postgres.port }}
      targetPort: {{ .Values.postgres.port }}
